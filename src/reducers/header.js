const initialState = {
  teste: 'aaakeehh',
  freeUser: "freeUser",
  vipUser: "vipUser",
  admin: "admin",
  band: "band",
}

export function header(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default header
